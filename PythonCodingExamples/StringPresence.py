# The find() method finds the first occurrence of the specified value.
# The find() method finds the first occurrence of the specified value.

str = "welcome to python programming"
sub_string = "python"

print(str.find(sub_string))

if str.find(sub_string) == -1:
    print("Not found")
else:
    print("Found")

